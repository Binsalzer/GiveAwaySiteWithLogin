@using GivaeAwaySiteWithLoginHw.Web.Models;
@using GiveAwayWithLoginHw.Data;
@model MyAccountViewModel


@foreach (Ad ad in Model.Ads)
{

    <div b-gyvb0mk0dj class="container" style="margin-top:80px;">
        <main b-gyvb0mk0dj role="main" class="pb-3">
            <div class="row">
                <div class="col-md-6 offset-md-3">
                    <div class="bg-light p-4 rounded shadow mb-3">
                        <h5>Listed on @ad.Date.ToLongDateString()</h5>
                        <h3>Details:</h3>
                        <p>@ad.Text</p>
                        <div>
                            <form method="post" action="/home/deleteAd">
                                <input type="hidden" name="id" value=@ad.Id />
                                <button class="btn btn-danger">Delete</button>
                            </form>
                            <div>
                            </div>
                        </div>
                    </div>
        </main>
    </div>
}

